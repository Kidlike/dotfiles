# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi

# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]; then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi
if ! [[ "$PATH" =~ "$HOME/.cargo/bin:" ]]; then
    PATH="$HOME/.cargo/bin:$PATH"
fi
export PATH

# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

### <Bash_configs> ###

export HISTSIZE=-1
export HISTFILESIZE=-1

# expand aliases on non-interactive shell
shopt -s expand_aliases

# ** matches all files and zero or more directories and subdirectories.
shopt -s globstar

# User specific aliases and functions
if [ -d ~/.bashrc.d ]; then
    for rc in ~/.bashrc.d/*; do
        if [ -f "$rc" ]; then
            . "$rc"
        fi
    done
fi
unset rc

### </Bash_configs> ###

source <(kubie generate-completion)

# sdkman
export SDKMAN_DIR="$HOME/.sdkman"
[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"

# If not running interactively, skip the rest
[ -z "$PS1" ] && return

# mise for auto selection of command versions (e.g. python, etc)
eval "$(mise activate bash)"

export EDITOR=vim
export MAVEN_OPTS='-Xms256M -Xmx2048M'
export GIT_HOOK_POST_CHECKOUT_MODE='clean'
export AWS_PROFILE='dev'

# starship.rs
eval "$(starship init bash)"

# colors
export LS_COLORS="$(vivid generate rose-pine-moon)"

# atuin for history and ctrl-r
. "$HOME/.atuin/bin/env"
[[ -f ~/.bash-preexec.sh ]] && source ~/.bash-preexec.sh
eval "$(atuin init bash --disable-up-arrow)"

# fzf (for ctrl-t)
FZF_CTRL_R_COMMAND= FZF_ALT_C_COMMAND= eval "$(fzf --bash)"

# carapace/tabdance for tab-completion
#source <(carapace _carapace)
source <(tabdance _tabdance)

# fzf-tab-completion
#source ~/.fzf-bash-completion.sh
#bind -x '"\t": fzf_bash_completion'
#export FZF_COMPLETION_AUTO_COMMON_PREFIX=true
## reduce flickering
# _fzf_bash_completion_loading_msg() { echo "${PS1@P}${READLINE_LINE}" | tail -n1; }

